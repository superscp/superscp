[pytest]
addopts = -ra
markers =
    unit: fast deterministic unit tests
    fuzz: property-based and randomized tests
    smoke: basic end-to-end local checks
    integration: external dependency tests such as docker
